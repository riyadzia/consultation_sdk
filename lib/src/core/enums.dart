enum TabType { income, expense, saving }

enum SummeryType { week, month, year }

enum DeleteResultType { success, error, noAction }

enum UserRole {generalDoctor, inhouseDoctor, patient, b2bPatient, operationNamage, operationReport, b2cAgent, pharmacyAgent, partnerAgent, pharmacyPartner, clinicPartner}
enum UserType {user, admin, partner}
